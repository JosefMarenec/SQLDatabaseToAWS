# Core AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# PostgreSQL / Redshift connectivity
psycopg2-binary>=2.9.9

# Data generation (seeding)
Faker>=24.0.0

# Analytics output formatting
tabulate>=0.9.0

# ETL / data processing (used in Glue jobs locally too)
pandas>=2.2.0
pyarrow>=15.0.0

# Progress bars
tqdm>=4.66.0

# Config parsing
configparser>=6.0.0

# AWS Glue local testing (optional, install only if running Glue jobs locally)
# awsglue-local>=1.0.0
